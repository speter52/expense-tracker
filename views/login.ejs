<!DOCTYPE html>
<html>
<head>
	<title>Expense Tracker</title>
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.99.0/css/materialize.min.css">
	<style>
        body {
            background-color: #f1f1f1;
        }
	</style>
</head>
<body>
    <nav style="overflow-y: hidden;">
        <div class="nav-wrapper blue">
            <a class="brand-logo" style="padding-left: 1vw;">Expense Tracker</a>
        </div>
    </nav>
    <div class="container" style="width: 30%;">

        <!-- LOGIN FORM -->
        <div class="section">
            <div class="col s12">
                <!-- show any messages that come back with authentication -->
                <% if (message.length > 0) { %>
                <div class="card-panel"><%= message %></div>
                <% } %>

                <form action="/login" method="post">
                    <div class="form-group">
                        <label>Username</label>
                        <input type="text" class="form-control" name="username">
                    </div>
                    <div class="form-group">
                        <label>Password</label>
                        <input type="password" class="form-control" name="password">
                    </div>
                    <div class="row">
                        <button type="submit" class="waves-effect waves-light btn col s2 offset-s10 blue">Login</button>
                    </div>
                </form>

                <hr>

                <p>Need an account? <a href="/signup">Sign up</a></p>
            </div>
        </div>
    </div>
</body>
</html>
